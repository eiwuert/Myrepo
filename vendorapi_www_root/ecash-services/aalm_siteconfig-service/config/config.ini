; override these values in override.ini in the same directory

config_dir="./config"

; only for initial config load (requires VPN connection to .tss)
management_host=reader.ecashaalm.ept.tss
management_user=ecash
management_pass=Hook6Zoh