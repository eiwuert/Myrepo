; override this file with override.ini in the same directory

; debug enabling
debug = false

; the customer_service wsdl location
customer_service.wsdl = "http://localhost:8080/customer_service/customerFlow?wsdl"

; the lead api wsdl location
lead_api.wsdl = "http://localhost:8080/customer_service/leadapi?wsdl"

captcha.static_value = "AAAAA"

; if present, these will be used as HTTP auth 
; and override credentials in the request
; username=
; password=

; character encoding; defaults to ISO-8859-1
; encoding="UTF-8"